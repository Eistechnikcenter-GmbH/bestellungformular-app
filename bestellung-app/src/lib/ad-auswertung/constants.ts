/** Odoo res.partner id for AGL Activ Services GmbH (leasing bank). */
export const AGL_PARTNER_ID = 577;

export const AGL_PARTNER_NAME = "AGL Activ Services GmbH";

/** Fallback tag when no CRM opportunity is linked. */
export const KALT_KONTAKT_TAG = "Kalt Kontakt";

/** Minimum fuzzy-match score (0–1) to accept a leasing-customer mapping. */
export const FUZZY_MATCH_THRESHOLD = 0.45;

/** Odoo credit-note prefix for Stornorechnungen (excluded from all reports). */
export const STORNO_INVOICE_PREFIX = "RINV/";

/** res.partner.category name for internal/CEO group companies (excluded from totals by default). */
export const EIGEN_CATEGORY = "Eigen";

/** res.partner.category for cases pending clarification (excluded from totals by default). */
export const KLAERUNG_CATEGORY = "in Klärung";
